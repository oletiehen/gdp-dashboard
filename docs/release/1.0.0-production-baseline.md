# Rehakompass 1.0.0 – unveraenderte Produktionsbasis

Stand: 22. Juli 2026, 17:38 Uhr CEST

## Schutzregel

Der bestehende Render-Dienst darf bis zu einer ausdruecklichen Produktionsfreigabe weder neu bereitgestellt noch umkonfiguriert werden. Das betrifft insbesondere Branch, Build- und Startbefehl, Umgebungsvariablen, Datentraeger, Domain, Daten und Schluessel.

## Lesend bestaetigte Fakten

| Merkmal | Befund | Nachweis |
| --- | --- | --- |
| Oeffentliche URL | `https://gdp-dashboard-lccm.onrender.com` | HTTP-Pruefung |
| Startseite | HTTP 200 | `curl` am 22.07.2026, 17:38 CEST |
| Health-Check | HTTP 200, `ok: true`, Version `1.0.0`, Runtime `node` | `/api/health` |
| Konfigurationsindikatoren | KI, Zugang, Synchronisierung und Push melden `configured: true` | `/api/health`; bestaetigt nur Konfiguration, nicht die End-to-End-Funktion |
| Geschuetzte Sitzung | direkter Aufruf ohne Sitzung liefert HTTP 401 | `/api/session` |
| Produktions-Remote-Branch | `origin/reha-kompass-premium-server` bei `dca2dd074b9199dc9be4637e526a1f0a3917a990` | Git-Fetch und `git rev-parse` |

## Noch nicht frisch ueber das Render-Dashboard bestaetigt

Die folgenden Angaben duerfen nicht aus `render.yaml` als aktueller Ist-Zustand abgeleitet werden. Fuer den Release-Gate-Bericht sind sie im authentifizierten Render-Dashboard erneut rein lesend zu erfassen:

- aktive Deploy-ID, aktiver Commit und Deploy-Zeitpunkt;
- tatsaechlicher Build- und Startbefehl des bestehenden Dienstes;
- Instanztyp und dauerhafter Laufstatus;
- Datentraeger, Groesse und Mount-Pfad;
- Namen der aktuell gesetzten Umgebungsvariablen;
- vorheriger erfolgreicher Render-Deploy als Plattform-Rollback.

Die lokale Browsersteuerung konnte die bestehende authentifizierte Firefox-Sitzung wegen eines macOS-ScreenCaptureKit-Fehlers nicht auslesen. Es wurde deshalb keine ungesicherte Aussage als bestaetigt markiert und keine Render-Einstellung veraendert.

## Sollkonfiguration aus dem Repository

`render.yaml` beschreibt fuer einen spaeteren, getrennten Kandidaten:

- Runtime: Node
- Build: `npm ci && npm run build`
- Start: `npm start`
- Health: `/api/health`
- Plan: Starter
- Region: Frankfurt
- Datentraeger: 1 GB unter `/var/data/rehakompass`

Diese Angaben sind Konfigurationsvorgaben und kein Nachweis der aktuellen Produktion.

## Freeze-Nachweis

Seit Beginn dieses Release-Auftrags wurden keine produktiven Render-Einstellungen, produktiven Geheimnisse, produktiven Daten, Domains, Datentraeger oder Produktionsbranches veraendert und kein Produktions-Deploy ausgeloest.

