# Rehakompass 1.0.0 – Render-Smoke-Test

Stand: 5. August 2026

## Aktualisierung nach ausdrücklicher Betriebsfreigabe

Die nachfolgend erhaltene Prüfung vom 22. Juli beschreibt den damaligen isolierten Release-Candidate-Plan. Nach späteren ausdrücklichen Freigaben wurde der bestehende Render-Dienst auf Starter umgestellt und mit persistentem Speicher ausgestattet. Diese produktive Betriebsprüfung ergänzt den historischen Kandidatenbericht; sie ersetzt keine weiterhin offene iPhone-Abnahme.

| Prüfung | Ergebnis | Anonymisierter Nachweis |
| --- | --- | --- |
| Aktueller automatisierter Check | bestanden | Lint; 20 aktive Unit-Tests bestanden, 1 bewusst übersprungen; 13 Integrationstests; Build; Audit mit 0 Schwachstellen |
| Aktiver Produktionscommit | bestanden | `8b5c29a15d6de011e9cdfbffad20b87407591e84` |
| Render-Deploy | bestanden | `dep-d9plas3m8hqs73fq37ng`, live |
| Starter-Dienst | bestanden | Render zeigt Node / Starter; kein Free-Tier-Spin-down |
| Build und Start | bestanden | `npm ci && npm run build`; `npm start` |
| Health-Check | bestanden | HTTP 200, `ok: true`, Version 1.0.0 |
| Datenträger | bestanden | 1 GB, `/var/data/rehakompass` |
| Laufender Datenpfad | bestanden | `DATA_DIR=/var/data/rehakompass` |
| Vorab-Sicherung | bestanden | 388 Byte, SHA-256 `6158fee9519a8413c87107a090076f018bb5070b1dea64381e37f4b4c24236a5`, Modus 0600 |
| Datenübernahme | bestanden | Quell- und Zielprüfsummen von `meta.json` und `push.enc.json` identisch |
| Neustart und Persistenz | bestanden | Dienstneustart protokolliert; Dateien danach vorhanden |
| Entschlüsselbarkeit | bestanden | verschlüsselte Push-Struktur vor und nach Neustart erfolgreich geöffnet; Inhalte nicht protokolliert |
| Schlüsselbehandlung | bestanden | OpenAI-, Zugangs-, Daten-, Sitzungs- und VAPID-Werte weder ausgegeben noch verändert |
| Öffentliche Oberfläche | bestanden | HTTPS-Seite lädt; Zugangsdialog wird angezeigt |
| Passkey | offen | Health meldet `passkeyConfigured: false`; Einrichtung auf Olafs Gerät erforderlich |
| Korrekter Login | offen | Zugangscode wurde nicht ausgelesen; manueller Erfolgstest erforderlich |
| KI-Erfolg und KI-Ausfall | offen | Konfiguration bestätigt, End-to-End-Fälle nicht erneut produktiv ausgelöst |
| Upload/Archiv/Sync/Löschung | offen | kein synthetischer Produktionsdurchlauf in dieser Betriebsprüfung |
| Service Worker / Offline | offen | automatisiert lokal grün; Produktions-PWA noch manuell zu prüfen |
| iPhone-Hintergrund-Push | offen | echter gesperrter iPhone-Test erforderlich |
| Getrennter Restore-Test | offen | Migration auf Produktionsdatenträger bestanden; getrenntes Wiederherstellungsziel fehlt |

Aktuelle betriebliche Bewertung: Der Render-Dienst ist stabil online und der persistente Speicher ist funktionsfähig. Die formale Vollabnahme bleibt wegen der offenen Geräte- und End-to-End-Tests `NO-GO`.

## Historischer Prüfstand vom 22. Juli 2026

## Identifikation

| Feld | Wert |
| --- | --- |
| Getesteter lokaler Commit | `10d74b4f7c3373d94be94ca6b35aa1f06dd1d7cf` |
| Kandidatenbranch | `release/rehakompass-1.0.0-rc` |
| Uebertragener Implementierungscommit | `f16071d9afa43535b6aaca46af70760ad901f7c6` mit identischem Git-Baum |
| Review | [Draft Pull Request #2](https://github.com/oletiehen/gdp-dashboard/pull/2), nicht gemergt |
| Kandidaten-Deploy-ID | blockiert – kein kostenpflichtiger Dienst ohne Freigabe erstellt |
| Kandidaten-URL | blockiert |
| Produktion | `https://gdp-dashboard-lccm.onrender.com`, waehrend der Pruefung unveraendert |

## Lokale und automatisierte Nachweise

| Pruefung | Ergebnis | Anonymisierter Nachweis |
| --- | --- | --- |
| Reproduzierbare Installation | bestanden | `npm ci`, 216 Pakete, 0 Schwachstellen |
| Lint | bestanden | `npm run lint` |
| Unit | bestanden | 19/19 |
| Integration | bestanden | 8/8 |
| Produktions-Build | bestanden | `build_complete`, Version 1.0.0 |
| E2E Mobil und Desktop | bestanden | 24/24 |
| PWA/Offline lokal | bestanden | App-Shell nach erstem Entsperren offline verfuegbar |
| Automatisierte Barrierefreiheit | bestanden | 2/2, keine schweren oder kritischen automatisierten Befunde |
| Produktionsabhaengigkeiten | bestanden | `npm audit --omit=dev --audit-level=high`, 0 Schwachstellen |
| Secret-/Profildaten-Scan | bestanden | keine Schluessel, Tokens oder verbotenen persoenlichen Marker im getrackten Code/Build |
| Kontrollierter Produktionsstart | bestanden | Unit-Tests fuer fehlende/ungueltige Pflichtvariablen |

Der erste parallel zum E2E-Lauf gestartete separate Barrierefreiheitstest kollidierte am lokalen Webserver-Port. Die Ursache war keine Produktabweichung; der Test wurde anschliessend allein wiederholt und bestand 2/2.

## Kandidatenpruefungen

| Pruefung | Status | Ergebnis / Blockade |
| --- | --- | --- |
| Build und Start auf Render | blockiert | getrennter Kandidat noch nicht kostenpflichtig aktiviert |
| Health-Check Kandidat | blockiert | keine Kandidaten-URL |
| Stabiler Prozess und Neustart | blockiert | keine Kandidaten-Instanz |
| Persistenz unter `/var/data/rehakompass` | lokal bestanden, Render blockiert | synthetischer Integrationstest bestanden; realer Render-Datentraeger fehlt |
| Geschuetzter Login | lokal bestanden, Render blockiert | richtig/falsch/leer, Direktaufruf, Cookie, Neustart, Logout lokal bestanden |
| KI-Erfolgsfall | lokal mit Mock bestanden, Render blockiert | echter Anbieteraufruf nur im getrennten Kandidaten erlaubt |
| KI-Ausfallmodus | lokal bestanden, Render blockiert | Timeout und Quotenfehler kontrolliert; Kandidaten-Ausfall noch zu simulieren |
| Upload/Archiv/Sync/Loeschung | lokal bestanden, Render blockiert | verschluesselte synthetische Datei ohne Klartextrest; Datentraeger-E2E offen |
| Service Worker/Offline-Neustart | lokal bestanden, Render blockiert | produktive Kandidaten-Auslieferung noch offen |
| Echter iPhone-Hintergrund-Push | blockiert | echter Kandidat und manuelle iPhone-Abnahme fehlen |
| Ruhezeit/neutrale Anzeige/Abmeldung | teilweise lokal bestanden, blockiert | neutrale Payload und Abmeldung automatisiert; reale iPhone-Abnahme fehlt |
| Verschluesseltes Backup und Restore | blockiert | produktive Sicherung und getrennte Wiederherstellung nicht durchgefuehrt |
| Logs ohne Geheimnisse/Personendaten | lokal bestanden, Render blockiert | Kandidatenlogs fehlen |

## Kandidatengeheimnisse

Neue `SESSION_SECRET`-, `DATA_ENCRYPTION_KEY`- und VAPID-Werte sowie ein synthetisches `PRIVATE_PROFILE_JSON` wurden ohne Ausgabe der Werte in `.data/render-rc-secrets.env` erzeugt. Die Datei hat Modus 0600 und wird durch `.gitignore` ausgeschlossen. `OPENAI_API_KEY` und `APP_ACCESS_CODE` wurden nicht gelesen, ausgegeben, rotiert oder veraendert; sie muessen spaeter sicher ueber die Render-Oberflaeche in den Kandidaten uebernommen werden.

## Verbleibende Risiken

- Kein isolierter Render-Kandidat und damit kein Beleg fuer reale Plattformkonfiguration, Persistenz und Logs.
- Kein echter KI-Anbieter-Erfolgsfall im Kandidaten.
- Kein realer iPhone-Hintergrund-Push, Ruhezeit- oder Abmeldetest.
- Kein produktives verschluesseltes Backup mit erfolgreichem Restore in ein getrenntes Ziel.
- Aktive Produktions-Deploy-ID, vorheriger erfolgreicher Render-Deploy und tatsaechliche Produktionskonfiguration noch nicht frisch rein lesend aus dem Dashboard erfasst.

## Historische Zwischenentscheidung vom 22. Juli 2026

`NO-GO – Produktion blieb zu diesem Zeitpunkt unveraendert`

Der Kandidat darf erst nach Kostenfreigabe fuer einen zusaetzlichen Starter-Webdienst plus 1-GB-Datentraeger erstellt werden. Danach sind alle als blockiert markierten Tests mit synthetischen Daten nachzuholen.
